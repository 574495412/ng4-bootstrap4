import { CalculateFeePipe } from './calculate-fee.pipe';

describe('CalculateFeePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateFeePipe();
    expect(pipe).toBeTruthy();
  });
});
